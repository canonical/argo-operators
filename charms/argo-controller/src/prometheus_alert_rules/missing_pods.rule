alert: ArgoWorkflowPodsMissing
expr: increase(pod_missing[5m]) > 0
for: 5m
labels:
  severity: critical
annotations:
  summary: "Missing workflow pods detected"
  description: "Detected {{ $value }} missing workflow pods in the last 5 minutes."
