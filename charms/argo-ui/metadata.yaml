name: argo-ui
summary: Container-Native Workflow Engine for Kubernetes
description: Container-Native Workflow Engine for Kubernetes
series: [kubernetes]
resources:
  oci-image:
    type: oci-image
    description: 'Backing OCI image'
    auto-fetch: true
    upstream-source: argoproj/argoui:v2.4.3
requires:
  service-mesh:
    interface: service-mesh
deployment:
  type: stateless
  service: omit
